namespace Core.Constants;

public enum SizeEnum
{
    XS = 1,   // Extra Small (xs)
    SM = 2,   // Small (sm)
    M = 3,    // Medium (m)
    LG = 4,   // Large (lg)
    XL = 5,   // Extra Large (xl)
    XXL = 6,  // Double Extra Large (xxl)
    XXXL = 7, // Triple Extra Large (xxxl)
}